{
  "author": "EverydayHero",
  "name": "test",
  "version": "0.0.2",
  "description": "Widgets",
  "license": "MIT",
  "main": "widgets.js",
  "dependencies": {},
  "devDependencies": {
    "browserify": "~5.12.0",
    "uniq": "~1.0.1",
    "jest-cli": "^0.1.18",
    "react": "^0.11.2",
    "react-tools": "^0.11.2",
    "uglify-js": "^2.4.15",
    "reactify": "^0.14.0"
  },
  "scripts": {
    "setup": "npm install && npm run build && npm test",
    "build": "npm run scripts",
    "build-dev": "npm run scripts-dev",
    "scripts": "NODE_ENV=production browserify ./src/widgets.js | uglifyjs -cm > ./public/scripts/widgets.js",
    "scripts-dev": "NODE_ENV=development browserify ./src/widgets.js > ./public/scripts/widgets.js",
    "test": "NODE_ENV=development node_modules/.bin/jest"
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  }
}
